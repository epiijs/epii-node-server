{
  "name": "@epiijs/server",
  "version": "2.0.0",
  "description": "A simple server framework.",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf build && rm -rf coverage && rm -rf .nyc_output",
    "build": "npm run clean && tsc",
    "lint": "eslint --ext .ts",
    "test": "eslint --ext .ts && nyc --reporter=lcov mocha test/index.js --exit",
    "lcov": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/epiijs/server.git"
  },
  "keywords": [
    "epii",
    "web",
    "server"
  ],
  "author": "Sartrey Lee <sartrey@163.com> (https://sartrey.cn)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/epiijs/server/issues"
  },
  "homepage": "https://github.com/epiijs/server#readme",
  "devDependencies": {
    "@epiijs/eslint-config": "^0.1.4",
    "@tsconfig/node18": "^18.2.0",
    "@types/node": "^20.4.1",
    "eslint": "^8.44.0"
  },
  "dependencies": {
    "@epiijs/config": "^0.1.0",
    "@epiijs/inject": "^0.6.0",
    "bl": "^6.0.3",
    "find-my-way": "^7.6.2",
    "glob": "^10.3.2"
  }
}
